package principal;

public class Principal {
    /**
     * Esse projeto tem foco em abordar inovações que vieram com o java 8 e que são consideradas
     * um grande passo na linguagem, estamos falando de lambdas, nova API de data, MethodReference,
     * DefaultMethods e Streams.
     *
     *======Lambdas===============
     * Uma lambda é uma classe anonima, o que queremos dizer quando falamos classe anonima? queremos
     * dizer que é uma classe que não tem nome, isso porque a utilizamos quando queremos
     * apenas fazer uma operação pequena e que provavelmente não vai ser reaproveitada.
     * ver "java8.Lambdas"
     *
     *=========Streams================
     * É interessante conhecer as lambdas antes das streams pq elas se complementam. As streams são
     * maneiras mais elegantes e legíveis de se iterar sobre coleções, onde você criar "streams/fluxos"
     * que podem ser encadeados e fazer lindas operações de se ler.
     * ver "java8.Streams"
     *
     *=======MethodReference==================
     * Continuando na onda da elegância nós temos o methodReference, que é uma maneira mais enxuta de
     * se trabalhar com lambdas que precisam de apenas um "argumento".
     * ver "java8.MethodReference"
     *
     */
}
